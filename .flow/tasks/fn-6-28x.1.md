# fn-6-28x.1 Add eslint-plugin-jsx-a11y to ESLint flat config

## Description

Install and configure eslint-plugin-jsx-a11y in the ESLint flat config with the recommended preset, scoped to JSX/TSX files, to enforce accessibility best practices at lint time.

## Acceptance

- [x] eslint-plugin-jsx-a11y listed in devDependencies
- [x] Recommended rules applied to `**/*.{jsx,tsx}` files in eslint.config.js
- [x] `pnpm exec eslint --print-config` shows jsx-a11y rules active
- [x] `pnpm lint` runs without config errors

## Done summary

eslint-plugin-jsx-a11y v6.10.2 was installed (commit 4835575) and configured in eslint.config.js using the flat config API:

- `jsxA11y.flatConfigs.recommended` spread into a config block scoped to `**/*.{jsx,tsx}`
- 34 jsx-a11y rules confirmed active via `pnpm exec eslint --print-config`
- Lint passes clean on all tsx files

## Evidence

- Commits: 4835575, 595e819
- Tests: pnpm exec eslint --print-config shows 34 jsx-a11y rules active, pnpm exec eslint runs clean on tsx files
- PRs:
