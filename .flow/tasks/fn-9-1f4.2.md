# fn-9-1f4.2 Add per-route error boundaries to App.tsx

## Description

TBD

## Acceptance

- [ ] TBD

## Done summary

## Summary: Add per-route error boundaries to App.tsx

### Changes

1. **Created `RouteErrorFallback`** (`src/components/errors/RouteErrorFallback.tsx`) — Full-page error card with retry and "Go to Dashboard" navigation for route-level errors.
2. **Enhanced `AppErrorBoundary`** (`src/components/errors/AppErrorBoundary.tsx`) — Added `fallbackRender` prop for dynamic error/retry passthrough alongside existing static `fallback`.
3. **Updated `App.tsx`** — Added 3 per-route error boundary wrappers:
   - `DashboardErrorBoundary` — wraps 17 dashboard routes
   - `AdminErrorBoundary` — wraps 5 admin routes
   - `PlatformErrorBoundary` — wraps 7 platform routes

### Testing

- All 154 tests pass
- No new TypeScript errors

## Evidence

- Commits:
- Tests: command, pass, summary
- PRs:
