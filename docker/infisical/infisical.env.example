# ===========================================
# Infisical Secrets Manager Configuration
# ===========================================
# Copy this file to .env and fill in values before starting the stack
#
# SECURITY KEYS GENERATION:
# Generate unique encryption keys with:
#   openssl rand -hex 32
#
# Generate secure database password with:
#   openssl rand -base64 24 | tr -d '=/+'
#
# ===========================================

# ===========================================
# Database Configuration
# ===========================================
INFISICAL_DB_USER=infisical
INFISICAL_DB_PASSWORD=<generate-secure-password>
INFISICAL_DB_NAME=infisical

# ===========================================
# Security Keys (REQUIRED - Generate unique keys)
# ===========================================
# These keys are critical for data encryption and authentication
# Generate with: openssl rand -hex 32
#
# ENCRYPTION_KEY: Used for encrypting secrets at rest
INFISICAL_ENCRYPTION_KEY=<32-byte-hex-key>

# AUTH_SECRET: Used for signing JWT tokens
INFISICAL_AUTH_SECRET=<32-byte-hex-key>

# ===========================================
# Site Configuration
# ===========================================
# For local development
INFISICAL_SITE_URL=http://localhost:8080

# For production, use HTTPS with your domain:
# INFISICAL_SITE_URL=https://secrets.yourdomain.com

# ===========================================
# Email Configuration (Optional - for production)
# ===========================================
# Uncomment and configure for production email notifications
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=your-smtp-username
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM_ADDRESS=noreply@example.com
