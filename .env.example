# ==============================================
# FreshTrack Pro - Environment Configuration
# ==============================================

# ----------------------------------------------
# Frontend API Configuration
# ----------------------------------------------
# URL of the self-hosted backend API server
# Default: http://localhost:3000 (local development)
# Production: Set to your deployed backend URL
VITE_API_URL=http://localhost:3000

# ----------------------------------------------
# Stack Auth Configuration
# ----------------------------------------------
# Stack Auth project ID - get from Stack Auth dashboard
# Required for user authentication and identity management
VITE_STACK_AUTH_PROJECT_ID=your-project-id

# Stack Auth publishable client key - get from Stack Auth dashboard
# Required for client-side authentication
VITE_STACK_AUTH_PUBLISHABLE_CLIENT_KEY=your-client-key

# ----------------------------------------------
# Feature Flags (Optional)
# ----------------------------------------------
# Enable verbose API call logging in browser console
# Set to 'true' for debugging API requests/responses
# Default: false
VITE_DEBUG_API=false

# ----------------------------------------------
# Legacy Supabase (Temporary - Phase 6+ Migration)
# ----------------------------------------------
# These are still used during migration for data operations
# that haven't been moved to the new backend yet.
# TODO Phase 6: Remove after complete backend migration
VITE_SUPABASE_PROJECT_ID=your-supabase-project-id
VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key
VITE_SUPABASE_URL=https://your-project.supabase.co

# ----------------------------------------------
# Notes
# ----------------------------------------------
# 1. Copy this file to .env.local and fill in your actual values
# 2. Never commit .env.local to version control (already in .gitignore)
# 3. Stack Auth credentials are required for authentication to work
# 4. Backend must be running on VITE_API_URL for API calls to succeed
# 5. See docs/LOCAL_DEV_ENV.md for local development setup
